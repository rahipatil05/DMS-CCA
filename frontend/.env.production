# Set this to your deployed API URL in production.
# If frontend and API are on the same origin (e.g. served by same server),
# leave VITE_API_URL empty so all /api/* calls are relative.
VITE_API_URL=
